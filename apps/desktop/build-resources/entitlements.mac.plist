<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS hardened runtime entitlements for the KMail desktop
  client.

  Every entitlement is the *minimum* needed for the SDK to run
  under Apple's hardened runtime + notarisation:

    - allow-jit / allow-unsigned-executable-memory:
        Required because Electron's V8 JIT generates executable
        code at runtime. Removing either breaks the renderer.
    - disable-library-validation:
        Required because the napi-rs `.node` addon is signed by
        our build cert (or unsigned in dev). Without this,
        hardened runtime refuses to dlopen the addon and the
        SDK fails to load at startup.
    - allow-dyld-environment-variables:
        Required for the Squirrel.Mac auto-updater's helper
        process to find the right framework version.

  We intentionally do NOT enable network-server, audio-input,
  camera, location, address-book, etc — KMail is an email
  client, not a multimedia conferencing app.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>

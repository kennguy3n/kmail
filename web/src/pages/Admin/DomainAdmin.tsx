/**
 * DomainAdmin is the domain management console.
 *
 * In Phase 2 it will drive the DNS Onboarding wizard (MX / SPF /
 * DKIM / DMARC / MTA-STS / TLS-RPT / autoconfig) — see
 * docs/PROPOSAL.md §9.3 — and render the verification state from
 * the `domains` table (docs/SCHEMA.md §5.3). In Phase 1 it is a
 * placeholder.
 */
export default function DomainAdmin() {
  return (
    <section>
      <h2>Domain admin</h2>
      <p>Not yet implemented.</p>
    </section>
  );
}

/**
 * TenantAdmin is the tenant-level admin console.
 *
 * In Phase 3 it will expose tenant lifecycle, plan changes, seat
 * management, and audit log views backed by the Tenant Service
 * (docs/ARCHITECTURE.md §7) and the `audit_log` table
 * (docs/SCHEMA.md §5.8). In Phase 1 it is a placeholder.
 */
export default function TenantAdmin() {
  return (
    <section>
      <h2>Tenant admin</h2>
      <p>Not yet implemented.</p>
    </section>
  );
}

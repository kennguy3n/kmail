apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kmail.fullname" . }}-config
  labels:
    {{- include "kmail.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.kmailApi.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "kmail.fullname" . }}-api
  labels:
    {{- include "kmail.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.kmailApi.service.type }}
  ports:
    - name: http
      port: {{ .Values.kmailApi.service.port }}
      targetPort: {{ .Values.kmailApi.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "kmail.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
